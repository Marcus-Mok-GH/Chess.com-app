// This file is kept for compatibility but Next.js is no longer the main frontend
// The application now uses Vite for the frontend

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;

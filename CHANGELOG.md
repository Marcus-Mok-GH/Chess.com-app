### Added
- Puzzles: the Lesson Concept card now teaches the generated position itself. Pollinations AI summarizes the concrete tactical pattern in the exact puzzle on the board (without revealing the solution) instead of the generic catalog lesson text, refreshing for every new puzzle.
- Admin account management: admins can search accounts by username or email and ban (with an optional reason), unban, or permanently delete them from a new Admin page. Banned users are signed out immediately and blocked from signing back in.
